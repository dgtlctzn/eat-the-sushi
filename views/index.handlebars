<div class="container">
    <div class="row">
        <div class="col-sm-12">
            <h1 class="text-center">Sushi</h1>
        </div>
    </div>
    <div class="row">
        <div class="col-sm-4">
            <h2>Menu</h2>
            <ul>
                {{#each sushi}}
                {{#unless eaten}}
                <li>
                    <button type="button" class="btn btn-primary">🍣 Eat Me!</button>
                    {{this.sushi_name}}
                </li>
                {{/unless}}
                {{/each}}
            </ul>
        </div>
        <div class="col-sm-4">
            <iframe src="https://giphy.com/embed/YYICBjQ6EqHss" width="480" height="360" frameBorder="0"
                class="giphy-embed" allowFullScreen></iframe>
        </div>
        <div class="col-sm-4">
            <ul>
                {{#each sushi}}
                {{#if eaten}}
                <li>
                    😋
                    Eaten!
                    {{this.sushi_name}}
                </li>
                {{/if}}
                {{/each}}
            </ul>
        </div>
    </div>
    <div class="row">
        <div class="col-sm-4">
        </div>
        <div class="col-sm-4">
            <form>
                <div class="form-group">
                    <label for="exampleInputEmail1">What kind of sushi would you like to add to the menu?</label>
                    <input type="email" class="form-control" id="exampleInputEmail1" aria-describedby="emailHelp">
                </div>
                <button type="submit" class="btn btn-primary">Submit</button>
            </form>
        </div>
    </div>
</div>